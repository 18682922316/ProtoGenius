---
document: Technical Design Document
standard: IEEE 29148-2018 (companion to SRS)
run_id: {{ run_id }}
srs: {{ srs_path }}
generated_by: ProtoGenius
---

# Technical Design Document

> Source task (verbatim):
>
> > {{ task }}

This document derives **only** from the frozen SRS at `{{ srs_path }}`. Any
discrepancy between this document and the SRS must be resolved by amending
the SRS first and re-running the TDD generator.

## 1. Traceability matrix

| SRS requirement | Design element(s) | Notes |
|-----------------|-------------------|-------|
{% if extra.get('traceability') %}
{% for row in extra['traceability'] %}
| `{{ row.req }}` | {{ row.elements }} | {{ row.notes }} |
{% endfor %}
{% else %}
| _(generated by the TDD drafter)_ | | |
{% endif %}

## 2. Component view

{% if extra.get('architecture_mermaid') %}
{{ extra['architecture_mermaid'] }}

{{ extra.get('architecture_prose', '') }}
{% else %}
_(insert Mermaid architecture diagram + accompanying prose here)_
{% endif %}

## 3. Interface definitions

{% if extra.get('interfaces_md') %}
{{ extra['interfaces_md'] }}
{% else %}
_(populated by `protogenius.docs.interfaces.render_interfaces_section`)_
{% endif %}

## 4. Data structures

{% if extra.get('data_structures_md') %}
{{ extra['data_structures_md'] }}
{% else %}
_(populated by the TDD drafter — Pydantic-style schemas or JSON Schema fragments are acceptable)_
{% endif %}

## 5. Deployment topology

The prototype must run on **Linux** and **Windows** without code changes.
Suggested deployment paths:

- **Linux** — containerized via Docker; the generated `Dockerfile` (when
  applicable) sits next to the prototype root.
- **Windows** — direct Python (or Node) execution; the README documents
  required toolchain versions.

## 6. Risk register

| ID | Risk | Source (common-challenge / research) | Owner | Mitigation |
|----|------|---------------------------------------|-------|------------|
{% if research.common_challenges %}
{% for challenge in research.common_challenges %}
| R-{{ loop.index }} | {{ challenge }} | research synthesis | TBD | TBD |
{% endfor %}
{% else %}
| _(populated by the TDD drafter)_ | | | | |
{% endif %}

## 7. Test alignment plan

The test generator consumes this document together with the SRS. Each design
element above MUST appear in the `refs` list of at least one generated test
case (see `tests/spec.yaml`).
