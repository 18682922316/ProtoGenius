---
document: Software Requirements Specification
standard: IEEE 29148-2018 §5
run_id: {{ run_id }}
generated_by: ProtoGenius
---

# Software Requirements Specification

> Source task (verbatim):
>
> > {{ task }}

## 1. Introduction

### 1.1 Purpose

This document specifies the requirements for the prototype generated by
ProtoGenius for the task above. The SRS is the authoritative description of
**what** the system must do; the companion Technical Design Document (TDD)
covers **how** it does it.

### 1.2 Scope

The prototype is intended as a *validated minimum viable system* on the
acceptance platforms listed in §1.5. Out-of-scope concerns (full production
hardening, multi-tenant scaling, etc.) are explicitly excluded.

### 1.3 Definitions, acronyms, abbreviations

| Term | Meaning |
|------|---------|
| SRS  | Software Requirements Specification (this document) |
| TDD  | Technical Design Document |
| LLM  | Large Language Model |
| MCP  | Model Context Protocol |

### 1.4 References

- IEEE 29148-2018 — Systems and software engineering — Life cycle processes — Requirements engineering.
- ProtoGenius v1 product requirements (frozen).
{% if research and research.academic %}
- Adopted academic research items: see Appendix C and the audit log.
{% endif %}

### 1.5 Acceptance platforms

The prototype is validated on:
{% for platform in ['linux', 'windows'] %}
- **{{ platform }}**{% endfor %}

macOS is explicitly out of v1 scope.

### 1.6 Overview

§2 paints the overall picture; §3 enumerates verifiable requirements; §4
maps each requirement to a verification method; §5 contains traceability and
research provenance.

---

## 2. Overall description

### 2.1 Product perspective

{% if chosen_stack %}
The prototype is implemented on **{{ chosen_stack.language }}** running on
**{{ chosen_stack.runtime }}** (chosen option: *{{ chosen_stack.name }}*).
{% else %}
The implementation tech stack will be selected from the tech-stack-analysis
phase. See Appendix A for the option set considered.
{% endif %}

### 2.2 Product functions

A condensed list of functional areas (each is expanded with REQ-FN-* entries
in §3).

{% if structured_requirements.get('functions') %}
{% for fn in structured_requirements['functions'] %}
- {{ fn }}
{% endfor %}
{% else %}
- _(populate during requirement understanding stage)_
{% endif %}

### 2.3 User characteristics

{{ structured_requirements.get('users', '_(populate during clarification)_') }}

### 2.4 Constraints

- Single-user prototype unless multi-user is an explicit functional requirement.
- Must run on both Linux and Windows.
- Generated test suite must execute under GitHub Actions without manual setup.

### 2.5 Assumptions and dependencies

- The user-supplied configuration provides credentials for any third-party
  API the prototype depends on.
- Network egress is unrestricted at runtime (no allow-list).

---

## 3. Specific requirements

> Each requirement carries a unique identifier of the form `REQ-<class>-NNN`.
> Verification methods are described in §4 and cross-indexed in Appendix B.

### 3.1 Functional requirements

{% if structured_requirements.get('functional') %}
{% for req in structured_requirements['functional'] %}
- **{{ req.id }}** — {{ req.text }}
{% endfor %}
{% else %}
_(populated by the SRS-drafter sub-agent from the structured requirement summary.)_
{% endif %}

### 3.2 External interface requirements

_(REST / RPC / CLI surfaces — populated by the SRS drafter; see TDD §3 for
contracts.)_

### 3.3 Performance requirements

_(latency, throughput and resource budgets, when applicable.)_

### 3.4 Design constraints

_(applicable standards, regulatory considerations, language / runtime locks.)_

### 3.5 Software system attributes

| Attribute      | Requirement |
|----------------|-------------|
| Reliability    | The demo must run end-to-end without manual intervention. |
| Portability    | Linux + Windows verified by CI. |
| Maintainability| Generated code is formatted by ruff / prettier. |
| Security       | No secrets are committed; environment variables only. |

### 3.6 Verification (overview)

Verification is performed by the auto-generated test suite. Each requirement
in §3 is paired with at least one test case in `tests/` whose `refs` field
contains the requirement id.

---

## 4. Verification methods

| Method     | Description                                              |
|------------|----------------------------------------------------------|
| Inspection | Manual review of the artifact.                           |
| Demo       | The behaviour is shown end-to-end.                       |
| Analysis   | Reasoned argument from documentation / static checks.    |
| Test       | Automated test (unit / integration / E2E) asserts.       |
| LLM-align  | Semantic alignment by the alignment reporter sub-agent.  |

Per §6.2 of the v1 contract, the LLM-alignment verdict is **supplementary** —
its false positives and false negatives are acceptable.

---

## 5. Appendix A: Tech-stack options considered

{% if structured_requirements.get('stack_options') %}
{% for option in structured_requirements['stack_options'] %}
- **{{ option.name }}** — {{ option.language }} on {{ option.runtime }}: {{ option.rationale }}
{% endfor %}
{% else %}
_(populated by the tech-stack analyzer.)_
{% endif %}

## 6. Appendix B: Verification cross-index

_(generated from `tests/spec.yaml` once the test generator runs.)_

## 7. Appendix C: Research provenance

See `audit.jsonl` for the complete citation list. The research adoption gate
was confirmed by the user prior to the drafting of this SRS.
